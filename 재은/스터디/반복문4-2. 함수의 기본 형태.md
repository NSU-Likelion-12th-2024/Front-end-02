191p  
1번:  
#for in 반복문  
0  
1  
2  
3  
#for of 반복문  
사과  
배  
귤  
바나나

2번:  
재할당 오류 발생, const를 let으로 바꾼다.

3번:

```js
let ouput = 1;
for (let i = 1; i <= 100; i++) {
  ouput *= i;
}
console.log(`1~100의 숫자를 모두 곱하면, ${ouput}입니다.`);
```

1~100의 숫자를 모두 곱하면, 9.33262154439441e+157입니다.

4번:

```js
let output = "";
const size = 5;

for (let i = 1; i < 6; i++) {
  {
    for (let j = 5; j > i; j--) output += " ";
    for (let k = 1; k <= i * 2 - 1; k++) output += "*";
    output += "\n";
  }
  if (i == size) {
    for (let i = 4; i > 0; i--) {
      for (let j = 0; j < size - i; j++) output += " ";
      for (let k = 1; k <= 2 * i - 1; k++) output += "*";
      output += "\n";
    }
    console.log(output);
  }
}
```

218p  
1번

```js
const multiplayAll = (start, end) => {
  let multiple = 1;
  for (let i = start; i <= end; i++) {
    multiple *= i;
  }
  return multiple;
};
console.log(multiplayAll(1, 2));
console.log(multiplayAll(1, 3));
```

2-1번:

```js
const max = function (array) {
  let output = array[0];
  for (const item of array) {
    if (output < item) {
      output = item;
    }
  }
  return output;
};
console.log(max([1, 2, 3, 4]));
```

2-2번:

```js
function max(...items) {
  let output = items[0];
  for (const item of items) {
    if (output < item) {
      output = item;
    }
  }
  return output;
}
console.log(max(1, 2, 3, 4));
```

2-3번:

```js
const max = function (first, ...rests) {
  let output;
  let items;

  if (Array.isArray(first)) {
    //Array.isArray로 자료가 배열인지 확인 (type of로는 배열 확인 불가능)
    output = first[0];
    items = first;
  } else if (typeof first === "number") {
    output = first;
    items = rests;
  }
  for (const item of items) {
    if (output < item) {
      output = item;
    }
  }
  return output;
};
console.log(`max(배열): ${max([1, 2, 3, 4])}`);
console.log(`max(숫자, ...): ${max(1, 2, 3, 4)}`);
```
